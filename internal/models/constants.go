package models

// Telegram system relay commands
const (
	CommandStart = "start"
	CommandHelp  = "help"
)

// Responses to the user
const (
	TextUnknownCmd = "Unknown command"
	WELCOME        = "WELCOME"
)
